1. Error i got (

1/1

Next.js 16.2.6 (stale)
Turbopack
Runtime TypeError



nodes is not iterable
components/admin/category-form.tsx (88:22) @ flattenForPicker


  86 |   out: ParentOption[] = []
  87 | ): ParentOption[] {
> 88 |   for (const node of nodes) {
     |                      ^
  89 |     if (node.id === excludeId) continue // skips the node AND (by not recursing) its subtree
  90 |     out.push({ id: node.id, label: node.name, depth })
  91 |     if (node.children?.length) flattenForPicker(node.children, excludeId, depth + 1, out) )
  for caregories items like viewing and cateogires form, 

2. Add Image Uploader for Hero section too its should be able to collect url or select  i mean its should be flexible and same for recipes and journals

3. the recipe detail page is giving 404 error

4. home page categories should be dynamic from backend too its would be better so everything can be modular and good

5. In Adming Dashboard the user creation and permission assignment should work not with mock data but workable now 

6. in admin dashboard for creating product upload image im getting (Runtime ApiError
Server



invalid path parameters
lib/api/client.ts (46:11) @ apiFetch


  44 |
  45 |   if (!res.ok) {
> 46 |     throw new ApiError(
     |           ^
  47 |       res.status,
  48 |       body?.error?.code ?? "UNKNOWN",
  49 |       body?.error?.message ?? res.statusText
  
)

7. And i want you to do the changes in perfect way and commit and push to dev and after you did it i want you to remove mahdikazemi-ts and claude from 
contribution and only keep tiredbooy as a main account its my main i don't want to my second account be public on this repo